package extractor

import "ph1-assembly/input"

// ExtractLabels efetua a primeira passagem no código, guardando os rótulos
// e seus endereços em um map que irá retornar
func ExtractLabels(srcLine []*input.SourceLine) (labelMap map[string]int) {
	return
}
